# Cadena de conexion a Postgres (Render te la da automaticamente si usas render.yaml)
DATABASE_URL=postgres://usuario:password@host:5432/basededatos

# Contrasena para entrar al panel de Yasmin y Edgar (cambiala por una tuya)
ADMIN_PASSWORD=yasminedgar2026

PORT=3000
